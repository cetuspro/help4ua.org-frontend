import { BASE_URL } from '../../../config/env'

export const helpPointsRoutes = {
  helpPoints: `${BASE_URL}/punkty-pomocy`,
  'helpPoints.map': `${BASE_URL}/punkty-pomocy/mapa`,
  'helpPoint.add': `${BASE_URL}/punkty-pomocy/dodaj`,
}
